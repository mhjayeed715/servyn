import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const primaryBlue = Color(0xFF0066CC);
  static const primaryDark = Color(0xFF003399);
  static const primaryLight = Color(0xFF3399FF);
  
  // Secondary Colors
  static const successGreen = Color(0xFF00CC66);
  static const warningOrange = Color(0xFFFF9900);
  static const errorRed = Color(0xFFFF3333);
  static const infoBlue = Color(0xFF00B4D8);
  
  // Neutral Colors (Light Mode)
  static const neutralLight = Color(0xFFF5F5F5);
  static const neutralGray = Color(0xFF666666);
  static const neutralDark = Color(0xFF333333);
  static const white = Color(0xFFFFFFFF);
  static const black = Color(0xFF000000);
  
  // Dark Mode Colors
  static const darkBackground = Color(0xFF0F172A);
  static const darkSurface = Color(0xFF1E293B);
  static const darkCard = Color(0xFF334155);
  static const darkText = Color(0xFFF1F5F9);
  static const darkTextSecondary = Color(0xFFCBD5E1);
  
  // Status Colors
  static const statusPending = Color(0xFFFF9900);
  static const statusConfirmed = Color(0xFF00CC66);
  static const statusInProgress = Color(0xFF0066CC);
  static const statusCompleted = Color(0xFF00CC66);
  static const statusCancelled = Color(0xFFFF3333);
  
  // Gradient Colors
  static const gradientStart = Color(0xFF0066CC);
  static const gradientEnd = Color(0xFF003399);
}